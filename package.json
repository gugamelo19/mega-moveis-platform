{
  "name": "mega-moveis-platform",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "pnpm --filter @mega-moveis/web dev",
    "dev:api": "pnpm --filter @mega-moveis/api dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.5.4"
  },
  "dependencies": {
    "react-icons": "^5.6.0"
  }
}
